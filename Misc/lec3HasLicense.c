#include <stdio.h>

int main() {
    int age; 
    int has_license;

    printf("age=%d\nhaslicense=%d\n", age, has_license);
    if (age >= 16 && has_license)
        printf("can drive/n")
        
    return 0; 
}
